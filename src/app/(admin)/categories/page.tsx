/**
 * Categories placeholder.
 * Not in the original implementation plan but linked from the sidebar,
 * so this avoids a 404. Replace with a real categories management view
 * in a later phase.
 */
export default function CategoriesPage() {
  return (
    <div className="pa-page-pad">
      <h1 className="pa-h1">Categories</h1>
      <p className="pa-muted">Coming soon.</p>
    </div>
  );
}
