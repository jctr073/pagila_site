/**
 * Default slot for the `@drawer` parallel route. Returns null when no
 * drawer/modal route is active. Required by Next 16 parallel routes.
 */

export default function DrawerDefault() {
  return null;
}
